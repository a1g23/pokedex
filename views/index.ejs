<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokedex</title>
</head>
<body>
    <%- include("./nav.ejs") %>
    <div class="indexContainer">
      <% for (let i = 0; i < pokemon.length; i++) { %>
      <a href="/index/<%= i %>">
        <div class="pokemonContainer">
          <div class="pokemonPicture"><img src="<%= pokemon[i].img %>" /></div>
          <div class="pokemonName"><%= pokemon[i].name %></div>
        </div>
      </a>
      <% } %>
    </div>
  </body>
</html>